-- displays the number of records with id=89 inthe table first_table of the database hbtn_0c_0 in your MySQL server.
-- the database name will be passed as an arguement of the mySQL command

SELECT COUNT(*)FROM first_table WHERE id=89;
